config:
    actions:
    -   name: set_ticket_status
        params:
            status: Answered
    -   name: attach_external_answer
    conditions:
        all:
        -   name: category
            operator: non_empty
            value: 'true'
name: default_answer
orderId: 100
rulesType: EmailReply
